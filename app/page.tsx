export default function Home() {
  return (
    <main>
      <h1>ws-git server</h1>
      <p>WebSocket git object sync protocol server.</p>
      <p>Use <code>wsgit://</code> URLs to push and fetch.</p>
    </main>
  );
}
